# 可选：覆盖 Go 二进制文件路径（支持绝对路径、相对路径或 ~ 展开）
# SMP_BINARY_PATH=~/social-media-models/smp

# 可选：覆盖模拟结果输出目录（支持绝对路径、相对路径或 ~ 展开）
# SMP_BASE_PATH=./run

# 可选：覆盖统计数据库路径（LMDB 目录，默认 $SMP_BASE_PATH/stats.lmdb）
# SMP_DB_PATH=./run/stats.lmdb

# 可选：覆盖图表输出目录（默认 $SMP_BASE_PATH/plots）
# SMP_PLOTS_PATH=./run/plots
